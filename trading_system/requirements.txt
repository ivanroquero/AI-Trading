mt5linux
pandas==2.2.3
numpy==2.1.3
# FIXED: pandas_ta — pinned beta is dead. Use latest from official repo (pure-Python, fully compatible with Python 3.10)
pandas_ta @ git+https://github.com/twopirllc/pandas-ta.git
scikit-learn==1.5.2
xgboost==2.1.2
tensorflow==2.17.0
joblib==1.4.2
pyarrow==17.0.0
pyyaml==6.0.2
tqdm==4.66.5
# supervisor is already installed via apt — removed from pip to avoid duplicate/conflict